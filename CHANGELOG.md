# DinoLab v0.29

- 모바일 화면의 상단 메뉴, 프로필 버튼, 결과 버튼 배치와 터치 영역 개선
- `브라우저에 저장` 계열 표현을 `임시 저장` 중심으로 정리
- 프로필 버튼 문구를 `임시 저장`, `저장된 프로필 불러오기`, `프로필 코드 복사`, `프로필 코드 붙여넣기`로 통일
- 구버전에 남아 있던 기본 프로필 `포차쿠차쓰리`를 최초 1회 자동 정리
- Open Beta 및 버전 정보를 계산기 제목과 하단에 표시
- PWA 서비스워커 캐시 버전 갱신

# DinoLab v0.27

- 상단의 큰 `스펙 입력하기` 버튼 삭제
- 실제 계산 버튼 3개를 히어로 영역에 배치
- 첫 번째 버튼 문구를 `50분 생존률 90% 이상 최고딜 조합 찾기`로 변경
- 계산 중지 버튼 삭제
- 서비스워커 캐시 및 앱 버전 갱신

# DinoLab v0.26
- HERO 문구를 “이헤와 함께하는 White 부족의 공식 Titan Research Lab”로 변경
- 실행 버튼 순서를 최고딜 조합 찾기 → 직접 선택한 5룬 계산 → 최대 말뚝 레벨로 변경
- 초기 닉네임 기본값을 빈칸으로 유지
- JSON 저장된 프로필 불러오기/내보내기 제거
- QR 생성/스캔 제거
- 프로필 코드 내보내기/저장된 프로필 불러오기만 유지

## v0.25
- 목표 시간을 분 단위로 직접 변경할 수 있는 입력란 추가
- 생존 시뮬레이션, 총딜, 최대 말뚝 탐색에 설정한 목표 시간 반영
- 결과 화면·복사·공유 문구에 설정한 시간 표시
- 앱/서비스워커 캐시 버전 v0.25로 갱신

# DinoLab 변경 내역

## v0.18
- 첫 화면 버튼을 `스펙 입력하기`로 명확하게 변경
- 버튼 클릭 시 계산기 입력 영역으로 즉시 이동하고 안내 메시지 및 강조 효과 표시
- 공개 기본 화면에서 개발자 닉네임·체력·공격력·별자리·타이탄 관련 스펙 제거
- 레벨캡, 기본 시뮬횟수, 목표 생존률만 공통 기본값으로 유지
- PWA 캐시 버전 갱신


## v0.17
- 첫 화면 우측 `계산 시작하기`를 실제 앵커 링크로 변경하고 스크롤 동작 보강
- QR 이미지 선택을 파일 입력과 직접 연결된 라벨 방식으로 변경
- 모바일에서 카메라만 강제되던 `capture` 속성 제거
- 정적 파일 캐시 버전을 v0.17로 갱신

# DinoLab v0.16

- 첫 화면 우측 `계산 시작하기` 버튼이 반응하지 않던 문제 수정
- 상단 메뉴와 시작 버튼의 화면 이동 이벤트를 초기화 단계에서 확실하게 연결
- 시작 버튼 클릭 시 계산기 입력 영역으로 이동 후 기본체력 입력칸에 초점
- PWA 캐시 버전을 v0.16으로 갱신해 기존 캐시가 남는 문제 방지
- 기존 v0.15 계산·프로필·QR 기능 유지

## v0.23
- JSON 저장된 프로필 불러오기/내보내기 복구: 빈 스펙에서도 내보내기 가능
- 같은 JSON 파일을 연속으로 다시 불러올 수 있도록 파일 입력 초기화
- BOM/빈 파일/잘못된 JSON 오류 메시지 보강
- 불러온 프로필을 브라우저 프로필 목록에도 저장
- Hero 영역에 White 부족 공식 Titan Research Lab 브랜딩 추가
- 상단 최고딜 버튼을 optimize 버튼에 직접 연결
- 앱/서비스워커 캐시 버전 v0.23으로 갱신
